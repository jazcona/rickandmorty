# Configuration for git-cliff changelog generator

[changelog]
header = """
# 📝 Changelog

Todos los cambios notables de Rick & Morty Explorer se documentarán aquí.

"""
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Sin publicar]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}**{{commit.scope}}**: {% endif %}{{ commit.message | upper_first }}\
    {% endfor %}
{% endfor %}\n
"""
trim = true
footer = """
---
🛸 **Rick & Morty Explorer** - Explora el multiverso desde tu escritorio
"""

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_preprocessors = [
    { pattern = '\((\w+\s)?#([0-9]+)\)', replace = "([#${2}](https://github.com/jazcona/rickandmorty/pull/${2}))"},
]
commit_parsers = [
    { message = "^feat", group = "🚀 Características" },
    { message = "^fix", group = "🐛 Correcciones" },
    { message = "^doc", group = "📚 Documentación" },
    { message = "^perf", group = "⚡ Rendimiento" },
    { message = "^refactor", group = "♻️ Refactorización" },
    { message = "^style", group = "🎨 Estilo" },
    { message = "^test", group = "🧪 Tests" },
    { message = "^chore\\(release\\): prepare for", skip = true },
    { message = "^chore\\(deps.*\\)", skip = true },
    { message = "^chore\\(pr\\)", skip = true },
    { message = "^chore\\(pull\\)", skip = true },
    { message = "^chore|^ci", group = "⚙️ Mantenimiento" },
    { body = ".*security", group = "🔒 Seguridad" },
    { message = "^revert", group = "◀️ Reverts" },
]
protect_breaking_commits = false
filter_commits = false
tag_pattern = "v[0-9].*"
skip_tags = "v0.1.0-beta.1"
ignore_tags = ""
topo_order = false
sort_commits = "oldest"